<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="sign-up.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Users Data</title>
</head>
<body>
    <table> 
     <head><h2>Subscibed Users</h2></head>
        <thead>
        
          <th><b>FIRST NAME</b></th>
          <th><b>LAST NAME</b></th>
          <th><b>EMAIL ADDRESS</b></th>
          <th><b>TAKE ACTION</b></th>
      </thead>
      <% fieldsDataArray.forEach(userDetails) %>
      <% function userDetails(data){ %>
         <tr>
       
        <td><%= data.firstName %></td>
        <td><%= data.lastName %></td>
        <td><%= data.email %></td>
        <td><form action="/delete" method="POST" id="delete-btn">
            <!-- note that the word "data" in the value property of the form below, we have just specified as in the td above. -->
            <button onClick="this.form.submit" name="entry" value="<%= data._id %>">Delete</button>
          </form></td>
      </tr>
       <% } %>
     
   
   </table>

    <style>
        table {
            background-color: #F1EEE9;
            border-spacing: 20px;
            padding: 150px;
            width: 50%;
            margin-left: auto;
            margin-right: auto;
    
        }
    
        thead {
            color: #3BACB6;
    
        }
        h2{
            margin-left: auto;
            margin-right: auto;
            width: 500px;
        }
    
        td {
            font-family: sans-serif;
            font-size: small;
            font-weight: lighter;
        }
        button{
            color: red;
            border-style: none;
        }
     </style>
    <!-- <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>"> -->
</body>
</html>